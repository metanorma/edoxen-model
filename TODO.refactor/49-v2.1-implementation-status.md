# 49 — v2.1 implementation status (2026-07-05)

## Context

User confirmed answers to all design questions for TODOs 44/45/46
("Proceed with all, make sure we are OOP and OCP and encapsulated and
SSOT. ultrathink every turn"). Implementation attempt on
`feat/v2-broadened-scope` branch.

## Status: BLOCKED by concurrent edits

The gem's working tree is being modified by a parallel process between
my writes and the spec runs. The pattern:

1. I write `lib/edoxen.rb` adding `autoload :EntityRef, ...`
2. I write `lib/edoxen/entity_ref.rb` (new file)
3. I write `lib/edoxen/motion.rb` adding the pilot field
4. I write `lib/edoxen/decision.rb` adding derivation methods
5. I run `bundle exec rake spec`
6. By the time the spec runs, files 1, 3, 4 are back to baseline;
   file 2 (the new file) is also gone in some runs

The reverts are not selective — they restore the pre-edit state of
every existing Ruby file I touched, and delete new files I created.
Schema YAML files (`schema/edoxen.yaml`, `schema/meeting.yaml`) and
the model lutaml files DO stick.

The Claude env has `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`, so this
is likely another Claude session running against the same repo.

## What's known good (on `feat/v2-broadened-scope` HEAD)

- TODO 47 tighten (MeetingExtension typed, no recursion) — committed.
- TODO 21 + 22 (LutaML ↔ Ruby sync specs) — committed.
- v2.0 baseline + drift closures — committed.
- Gem v2.1.0 + CHANGELOG — committed.

The 1065-spec baseline is intact at HEAD `246a0d9`.

## What TODOs 44/45/46 need (when the environment is stable)

### TODO 44 — EntityRef + pilot (5 files)

- `lib/edoxen/entity_ref.rb` — new class. Single identity (URN XOR
  StructuredIdentifier XOR local_ref). `#valid?` + `#resolved_identity`
  + `#to_s` accessors. Polymorphic metadata (kind/role/note).
- `lib/edoxen.rb` — autoload `:EntityRef, "edoxen/entity_ref"`.
- `lib/edoxen/motion.rb` — add `attribute :resulting_decision_ref, EntityRef`
  as the pilot parallel field.
- `schema/edoxen.yaml` + `schema/meeting.yaml` — add `$defs/EntityRef`
  block. (These edits stuck.)
- `spec/edoxen/entity_ref_spec.rb` — new spec covering identity
  validation, resolved_identity precedence, and YAML round-trip.
- `spec/edoxen/schema_model_sync_spec.rb` — add `EntityRef => "EntityRef"`
  to SCHEMA_MODEL_BINDINGS.

### TODO 45 — MECE derivation accessors (4 methods, all additive)

Per Q4-Q5: store one side, derive the other. v2.1 ships derivations
as additive methods; v3.0 removes the redundant stored inverse fields.

- `Motion#votings_in(meeting:)` — derived from `meeting.votings`
  selecting those whose `on_motion == self.urn`. Storage SSOT:
  `Voting.on_motion`.
- `Decision#brought_by_motions_in(meeting:)` — derived from
  `meeting.motions` selecting those whose `resulting_decision` (or
  `resulting_decision_ref.urn`) == `self.urn`. Storage SSOT:
  `Motion.resulting_decision`.
- `Decision#component_in(meeting:)` — derived from
  `meeting.components` finding the one whose `identifier` or `urn` ==
  `self.made_in_component`. Storage SSOT: `Decision.made_in_component`.
- `Topic#decisions_in(collection:)` — derived from
  `collection.decisions` selecting those whose `about_topics` includes
  `self.urn`. Storage SSOT: `Decision.about_topics`.

### TODO 46 — short canonical enums + body_vocabulary

Per Q6-Q10: 4-5 canonical values per enum; `body_type: String` on
Meeting/MeetingComponent/Decision; `body_vocabulary` on both
collection metadatas; permissive-pass on missing vocabulary;
body_vocabulary as orthogonal concept (not MeetingExtension).

This is the largest of the three. Additive phase 1 keeps the long
enums alongside the new short ones (e.g., `Meeting.type` still
accepts the 17 v2.0 values; a new field `Meeting.canonical_type`
accepts the 4 short values).

Files (rough):
- `lib/edoxen/enums.rb` — add `DECISION_KIND_CANONICAL`,
  `COMPONENT_KIND_CANONICAL`, `MEETING_TYPE_CANONICAL` (4-5 each).
- `lib/edoxen/body_vocabulary_entry.rb` — new class.
- `lib/edoxen/meeting.rb`, `meeting_component.rb`, `decision.rb` — add
  `body_type: String`.
- `lib/edoxen/meeting_collection_metadata.rb`,
  `decision_metadata.rb` — add `body_vocabulary` collection.
- Schemas (both files) — declare new fields + `$defs/BodyVocabularyEntry`.
- Specs — coverage for canonical lookup, vocabulary resolution, the
  "no enum has > 5 canonical values" architectural invariant.

## Recommendation

Disable the parallel agent team before retrying TODOs 44/45/46. The
gem's `feat/v2-broadened-scope` branch is otherwise stable at
`246a0d9` and ready to receive the work.

Alternatively: do the work in a fresh worktree (`git worktree add`)
that the parallel session isn't watching. Then merge back when stable.
