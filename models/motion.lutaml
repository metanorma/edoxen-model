class Motion {
  identifier: String
  urn: String
  text: LocalizedString[0..*]
  mover: Person
  seconders: Person[0..*]
  status: MotionStatus
  introducedAt: DateTime
  proposedDecision: String
  resultingDecision: String
  // Pilot EntityRef field (1.0, TODO.refactor/1.0-design). Parallel to
  // `resultingDecision` (String). Prefer the typed form in new code;
  // the bare String form is removed in 1.0.
  resultingDecisionRef: EntityRef
  votings: String[0..*]
  extensions: MeetingExtension[0..*]
}
