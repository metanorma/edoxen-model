// Per-language canonical source URL (e.g. one PDF per language).
// `spelling` is an ISO 24229 spelling/conversion system code that
// identifies the language/script of the referenced document.
class SourceUrl {
  ref: String
  format: String
  spelling: String
  kind: SourceUrlKind
}
